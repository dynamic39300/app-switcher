# app-switcher 领域语言

这里只记录领域词义和关系，不存技术方案、临时任务或聊天记录。

## Language

**候选 App（candidate app）**：正在运行、具有普通应用身份、且非本应用自身的 App 进程；同一产品的不同运行进程是不同候选。
_Avoid_：把后台进程、已安装但未运行的 App 混为候选，或把同一产品名等同于同一个运行进程。

**切换目标（switch target）**：一个可选择的 App 或具体窗口；每个已分配键位只对应一个目标。
_Avoid_：把显示名称、窗口标题或临时编号当作目标身份。

**应用模式（app mode）**：覆盖层默认模式，每个候选 App 只占一个键位；选择时请求该 App 重新呈现正常界面并转到前台，由系统和 App 决定具体显示哪个窗口。
_Avoid_：把应用模式理解成精确窗口选择。

**窗口模式（window mode）**：按当前能力把可识别且可控制的窗口展开为独立目标的模式；某 App 存在不确定窗口时，该 App 整体保留为一个应用入口。
_Avoid_：把所有可枚举窗口都算作可精确切换目标。

**窗口标题（window title）**：帮助用户辨认窗口的当前名称；标题可变化、可为空、可重复，不决定窗口身份。
_Avoid_：用「窗口 1 / 2」暗示长期稳定的业务身份。

**应用回退（app fallback）**：窗口模式中，无法确认窗口能力或无法辨认窗口的 App 以一个应用入口呈现。
_Avoid_：把选择具体窗口后偷偷激活另一个窗口称为回退成功。

**键位（key / keycap）**：覆盖层键盘上的一个物理位置，包括字母 `A–Z` 与数字行 `1–0 - +`，共 38 个。
_Avoid_：把覆盖层内的键位与唤起覆盖层的全局快捷键混用。

**键位映射（key mapping）**：键位到切换目标的确定性对应关系；在一次已显示的快照内固定，下一次唤出或切模式时可重算。
_Avoid_：把当前映射当作永久固定键位。

**rank**：候选的使用排序，以激活次数、最近激活和 App 身份等规则确定优先级。
_Avoid_：把 rank 等同于风险等级或产品优先级。

**覆盖层（overlay）**：由唤出键触发、展示模式和键位映射的键盘形状面板。
_Avoid_：把覆盖层称作弹出菜单或 Dock 替代。

**唤出键（trigger）**：用于打开或关闭覆盖层的全局按键组合或手势，与覆盖层内的目标选择键分开。
_Avoid_：把唤出键写成某个永远不变的快捷键值。

**快捷键草稿（shortcut draft）**：设置窗口中正在编辑的唤出组合与手势开关；保存成功前不替代原设置，关闭窗口即放弃未保存修改。
_Avoid_：把录制完成等同于设置已生效。

**就近让位（collision fallback）**：候选首字母键冲突时，较低优先级候选落到附近空闲字母键的自动分配策略。
_Avoid_：把就近让位说成随机分配或用户手动固定。


## 商业服务

**账号（account）**：拥有 AppSwitcher 使用权益、订单和支持记录的统一用户身份；可在多个已支持设备上使用。
_Avoid_：把微信/支付宝付款身份或单个安装实例当作 AppSwitcher 账号。

**使用权益（entitlement）**：账号在一段时间内使用核心切换功能的资格，来源于试用、已付款订单或有记录的补偿。
_Avoid_：把已登录、付款页面回跳或未验证付款截图当作权益生效。

**订单（order）**：某账号按确定价格购买确定期限的一次交易记录；支付、权益发放和退款分别有状态。
_Avoid_：把一次性月费购买称为已授权自动扣款。

**连续期限（continuous term）**：试用及已安排付费权益没有间断连接的一段可用时间；新增购买接在已有安排的末尾。
_Avoid_：每次购买都覆盖原到期日，或把到期后的停用空档补算为已购买时间。

**本地授权（local license）**：客户端可离线验证的限时使用证明，有自己的验证截止且不超过账号已获批权益。
_Avoid_：把离线验证期限理解为到期后免费赠送期，或把缓存到期当作服务端账号必然到期。

**登录会话（session）**：某次已授权的设备登录关系，用户可查看和撤销；数量不构成使用额度。
_Avoid_：用设备解绑或同时在线限制变相收紧不限设备的授权。
